import { OgImage, size, contentType } from "./og-image";

export { size, contentType };
export const alt = "consolestore — order food from your terminal";

export default function Image() {
  return OgImage();
}
