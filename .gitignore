# Brainstorming / visual-companion artifacts (local only)
.superpowers/

# Secrets & local config — NEVER commit (Swiggy creds, tokens, KMS keys)
.env
.env.*
*.local
config/local.*
secrets/
*.pem
*.key
.ssh/

# Go build output
/sshd
/broker
/bin/
/dist/
*.exe
*.test
*.out
coverage.*

# Go workspace
go.work
go.work.sum

# Local databases (dev SQLite)
*.db
*.sqlite
*.sqlite3

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# build artifacts (native binaries)
/store
/safestore

# runtime + debug-capture logs — never publish (may contain addresses/order data)
*.log

# SDD orchestration scratch
.superpowers/

# Dev-process docs (specs/plans) — keep local, out of the repo
docs/superpowers/
